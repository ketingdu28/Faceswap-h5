# XMeta 换脸 H5 — 产品交接文档

> 目标读者：运营/运维人员（无需前端开发背景）
> 最后更新：2026-05-27

---

## 1. 产品概述

XMeta 换脸 H5 是一个基于 AI 换脸技术的 Web 应用，用户上传自拍后选择游戏场景模板，AI 自动将用户面部替换到场景角色中，生成带前景特效的合成图片并可下载保存。同时支持生成皮克斯风格卡通头像和游戏纪念证书。

**线上地址：** `https://faceswap-h5.vercel.app`

**核心流程：** 打开页面 → 上传自拍（或拍照） → 选择场景风格（A/B/C × 3 种） → AI 生成换脸图 → 合成前景特效 → 下载（选配有证书生成）

---

## 2. 技术栈与部署

| 项目 | 说明 |
|------|------|
| 前端框架 | Vue 3 + TypeScript + Vite 8 |
| 部署平台 | Vercel（美国机房） |
| AI 服务 | 火山引擎即梦 / Doubao Seedream（国内机房） |
| 图片托管 | ImgBB（用户图 + 模板底图中转） |
| 包管理器 | npm |

**本地运行（仅开发/预览需要）：**
```
npm install
npm run dev        # 启动开发服务器，默认 http://localhost:5173
npm run build      # 构建生产包到 dist/
```

---

## 3. 页面路由

| 路径 | 页面 | 说明 |
|------|------|------|
| `/` | 主界面（PreStageV2） | 上传自拍、选择场景、触发换脸 |
| `/pre-stage` | 旧版主界面 | 已废弃，保留兼容 |
| `/result-stage` | 结果展示页 | 换脸结果预览、前景合成、下载、证书生成 |

---

## 4. 可替换资源清单

### 4.1 场景底图（AI 换脸模板）— 通过环境变量配置

9 张高清底图（对应场景选择器 A1~C3），存储为**国内可访问的 CDN URL**，通过 Vercel 环境变量配置：

| 场景编号 | 场景名称 | 环境变量名 | 当前值 |
|----------|----------|------------|--------|
| 1 | A1 | `VITE_AI_TEMPLATE_A1` | 待填写 |
| 2 | A2 | `VITE_AI_TEMPLATE_A2` | 待填写 |
| 3 | A3 | `VITE_AI_TEMPLATE_A3` | 待填写 |
| 4 | B1 | `VITE_AI_TEMPLATE_B1` | 待填写 |
| 5 | B2 | `VITE_AI_TEMPLATE_B2` | 待填写 |
| 6 | B3 | `VITE_AI_TEMPLATE_B3` | 待填写 |
| 7 | C1 | `VITE_AI_TEMPLATE_C1` | 待填写 |
| 8 | C2 | `VITE_AI_TEMPLATE_C2` | 待填写 |
| 9 | C3 | `VITE_AI_TEMPLATE_C3` | 待填写 |

**如何更换底图：**
1. 将新底图上传到 ImgBB（https://imgbb.com），获取 Direct Link（格式：`https://i.ibb.co/xxx/image.png`）
2. 进入 Vercel Dashboard → 项目 Settings → Environment Variables
3. 将对应环境变量的值替换为新链接
4. 重新部署（Vercel 会自动触发）

> **重要：** 即梦服务器在国内，无法访问 Vercel 域名或本地 localhost。底图必须使用国内可达的公网 CDN（ImgBB 可用但不稳定，推荐阿里云 OSS / 腾讯 COS）。

### 4.2 场景前景图（透明叠加层）— 本地文件替换

9 张透明 PNG 前景遮罩，覆盖在换脸结果上方形成最终合成图。

**文件路径：** `public/scenes/scene-{1-9}-fg.png`

| 文件 | 说明 | 推荐尺寸 |
|------|------|----------|
| `public/scenes/scene-1-fg.png` | A1 前景 | 941×1672 |
| `public/scenes/scene-2-fg.png` | A2 前景 | 941×1672 |
| `public/scenes/scene-3-fg.png` | A3 前景 | 941×1672 |
| `public/scenes/scene-4-fg.png` | B1 前景 | 941×1672 |
| `public/scenes/scene-5-fg.png` | B2 前景 | 941×1672 |
| `public/scenes/scene-6-fg.png` | B3 前景 | 941×1672 |
| `public/scenes/scene-7-fg.png` | C1 前景 | 941×1672 |
| `public/scenes/scene-8-fg.png` | C2 前景 | 941×1672 |
| `public/scenes/scene-9-fg.png` | C3 前景 | 941×1672 |

**如何更换前景图：**
1. 准备新前景 PNG（必须带透明通道，尺寸建议 941×1672，9:16 比例）
2. 命名为 `scene-{编号}-fg.png`（如 `scene-1-fg.png` 对应 A1）
3. 替换 `public/scenes/` 目录下同名文件
4. 提交代码 → Vercel 自动部署

**代码引用位置：** `src/components/SceneComposite.vue` 第 15-25 行（`SCENE_CONFIG` 数组）


### 4.3 证书模板

**Canvas 本地合成模板（当前在用，降级方案）：**
- `src/assets/certificate-template.png` — 证书底图，Canvas 直接叠加生成
- `src/assets/certificate-template.webp` — WebP 版本
- 代码引用：`src/components/AgentCertificate.vue` 第 3 行

**AI 合成模板（可选，需配置环境变量）：**
- 环境变量 `VITE_CERTIFICATE_TEMPLATE_URL` — 证书模板公网 URL
- 必须托管在国内 CDN（阿里云 OSS / 腾讯 COS），即梦服务器无法可靠拉取 ImgBB
- 证书蒙版 `VITE_JIMENG_CERT_MASK_URL` — `public/cert-mask.png` 或 CDN URL

**如何更换证书模板：**
- **Canvas 模式（推荐，无需额外配置）：** 直接替换 `src/assets/certificate-template.png` 文件，保持文件名不变
- **AI 模式：** 将模板上传到国内 CDN，更新 `VITE_CERTIFICATE_TEMPLATE_URL` 环境变量

### 4.4 场景风格预览图（选择器界面展示）

位于 `src/assets/` 目录，用于场景选择器中的风格卡片缩略图：

| 文件 | 对应风格 |
|------|----------|
| `src/assets/style-a.webp` | A 风格封面 |
| `src/assets/style-b.webp` | B 风格封面 |
| `src/assets/style-c.webp` | C 风格封面 |
| `src/assets/template-a-1.webp` ~ `template-a-3.webp` | A 系列 3 个场景缩略图 |
| `src/assets/template-b-1.webp` ~ `template-b-3.webp` | B 系列 3 个场景缩略图 |
| `src/assets/template-c-1.webp` ~ `template-c-3.webp` | C 系列 3 个场景缩略图 |

**如何更换：** 替换同名文件即可。代码引用在 `src/views/PreStageV2.vue` 中。

### 4.5 其他 UI 素材

| 文件 | 用途 |
|------|------|
| `public/favicon.svg` | 浏览器标签页图标 |
| `public/portal-bg.webp` | 主界面背景图（传送门按钮底图） |
| `public/magic-bg.webp` | 魔法/特效背景 |
| `public/cert-bg.webp` | 证书页背景 |
| `public/hero-placeholder.png` | 主视觉占位图（未上传自拍时的默认图） |

---

## 5. 场景滤镜调节

滤镜参数通过环境变量控制，影响最终合成图的饱和度、对比度、亮度：

| 参数 | 环境变量 | 默认值 | 说明 |
|------|----------|--------|------|
| 饱和度 | `VITE_SCENE_FILTER_SATURATE` | 135 | 100=不变，>100 增强 |
| 对比度 | `VITE_SCENE_FILTER_CONTRAST` | 105 | 100=不变，>100 增强 |
| 亮度 | `VITE_SCENE_FILTER_BRIGHTNESS` | 100 | 100=不变 |

**调节方式：** 在 Vercel Dashboard → Environment Variables 中修改对应变量值，重新部署生效。

**代码内置默认值位置：** `src/components/SceneComposite.vue` 第 43-45 行（当环境变量未配置时生效）

---

## 6. 功能开关

| 功能 | 环境变量 | 可选值 | 说明 |
|------|----------|--------|------|
| 应用模式 | `VITE_APP_MODE` | `FULL` / `TEASER` | FULL=完整功能+下载 / TEASER=仅预览，隐藏下载 |
| API 模式 | `VITE_API_MODE` | `mock` / `cloud` | mock=模拟数据 / cloud=真实 AI 服务 |
| AI 提供商 | `VITE_CLOUD_PROVIDER` | `jimeng` / `piapi` / `backend` / `invoker` | jimeng=即梦（推荐）/ invoker=小程序桥 |
| 姿势引导 | `VITE_POSE_GUIDE` | `1` / `0` | 是否启用拍照姿势引导界面 |
| 换脸分辨率 | `VITE_JIMENG_SIZE` | `1024x1024` / `2k` / `4k` | 分辨率越高越慢 |
| 证书尺寸 | `VITE_JIMENG_CERT_SIZE` | `1882x3344` | 证书输出分辨率 |

---

## 7. 常见问题排查

| 症状 | 可能原因 | 解决方向 |
|------|----------|----------|
| 换脸生成失败 | 火山引擎 Key 过期或额度用尽 | 检查 [火山引擎控制台](https://console.volcengine.com/ark) |
| 前景图没收上来 | Jimeng CDN 无 CORS 头导致 Canvas 被污染 | 正常现象，已自动降级为 CSS 双层叠加展示 |
| 生成图片有水印 | 即梦/豆包模型自带水印 | 目前无法去除，底层模型行为 |
| 场景底图加载失败 | 底图 URL 国内不可达或过期 | 重新上传 ImgBB 获取新链接，更新环境变量 |
| 证书生成失败 | 证书模板 URL 不可达 | 确认模板托管在国内 CDN（非 ImgBB） |
| Vercel 部署预览异常 | 环境变量未在 Vercel 配置 | Vercel 不会读取 `.env.local`，所有变量须在 Dashboard 配置 |
| 图片上传超时 | 原图过大 | 代码已自动压缩至 1280px 内，若仍超时检查 ImgBB Key |

---

## 8. 关键外部平台账号

| 平台 | 用途 | 控制台地址 |
|------|------|------------|
| Vercel | H5 页面部署 | https://vercel.com |
| 火山引擎 Ark | 即梦 AI 换脸服务 | https://console.volcengine.com/ark |
| ImgBB | 图片中转 CDN | https://api.imgbb.com |

---

## 9. 目录结构速查

```
xmeta-agent-h5/
├── public/
│   ├── scenes/                  ← 场景前景图（9 张，直接替换）
│   │   ├── scene-1-fg.png       （A1 前景）
│   │   ├── scene-2-fg.png       （A2 前景）
│   │   ├── ...
│   │   └── scene-9-fg.png       （C3 前景）
│   ├── cert-mask.png            ← 证书蒙版
│   ├── portal-bg.webp           ← 主界面背景
│   └── ...
├── src/
│   ├── assets/
│   │   ├── certificate-template.png  ← 证书模板（直接替换）
│   │   ├── template-a-*.webp    ← 场景缩略图（直接替换）
│   │   └── style-*.webp         ← 风格封面图（直接替换）
│   ├── components/
│   │   ├── SceneComposite.vue   ← 场景合成核心组件
│   │   ├── AgentCertificate.vue ← 证书生成组件
│   │   └── ...
│   ├── services/
│   │   └── cloudFaceSwapClient.ts ← AI 换脸接口
│   └── views/
│       └── PreStageV2.vue        ← 主界面
├── .env.example                 ← 环境变量说明（仅参考，不生效）
├── vercel.json                  ← Vercel 部署配置
└── vite.config.ts               ← 构建配置
```

---

## 10. 日常运营 Checklist

- [ ] **换场景底图：** 上传新底图到 ImgBB → 更新对应的 `VITE_AI_TEMPLATE_*` 环境变量 → 重新部署
- [ ] **换场景前景图：** 替换 `public/scenes/scene-{编号}-fg.png` → 提交部署
- [ ] **换证书模板：** 替换 `src/assets/certificate-template.png` → 提交部署
- [ ] **调节滤镜：** 修改 `VITE_SCENE_FILTER_*` 环境变量 → 重新部署
- [ ] **切换到演示模式（隐藏下载）：** 设置 `VITE_APP_MODE=TEASER` → 重新部署
- [ ] **换封面/缩略图：** 替换 `src/assets/` 下对应 webp 文件 → 提交部署
